@model FullStackTechTest.Models.Import.IndexViewModel
@{
    ViewData["Title"] = "Import JSON Page";
}

<div class="container">
    <a class="btn btn-primary" href="@Url.Action("Import","YourController")">Import JSON</a>

    @using (Html.BeginForm("Edit", "Import", FormMethod.Post, new { @class = "form-inline" }))
    {
        

        @if (true)
        {
            <div>
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        }
    }
</div>
